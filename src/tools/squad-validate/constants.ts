export const SQUAD_VALIDATE_DESCRIPTION = `Validate a SQUAD.yaml manifest against the schema and check referential integrity.

Checks: schema validation, prompt_file existence, agent name format (kebab-case), default_executor/reviewer references, and skill references.`
