export const STORY_READ_DESCRIPTION = `Read and parse a story markdown file into structured data.

Returns JSON with the story title, status, tasks, and parsed sections (acceptance criteria, file list, dev notes).`
