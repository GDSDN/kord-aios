export const KORD_METHODOLOGY_RULES = `## Methodology Advisory Rules

- **Stories-first by default**: Stories are the default execution unit. Opt out only when the user explicitly requests non-story execution.
- **Template source of truth**: Generate and shape artifacts from templates in \`.kord/templates/\`.
- **Pipeline order**: Run the workflow as PRD (\`@pm\`) -> Epic -> Stories (\`@sm\`) -> Validation (\`@po\`) -> Implementation -> QA.
`
