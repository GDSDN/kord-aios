- Recovery framing is correct: current workflow engine is a partial MVP foundation, not Synkra parity.
- Catalog breadth is the first hard gap: required 14 workflows vs 2 simplified builtins.
- Asset strategy must shift from inline TS strings to YAML-backed builtin workflows for maintainability and parity.
- Subagent question constraints confirm elicitation must stay orchestrator-owned.
- Existing storage groundwork is reusable, but richer state/reporting parity still requires schema and boulder-summary expansion.
- Recovery audits must classify scaffolding separately from parity-complete behavior to avoid false completion claims.
- Runtime fidelity depends on Synkra-like execution depth (main-session elicitation, JIT prompt assembly, fan-out/join, and deterministic gates), not just command wiring.
- Builder-owned continuation is the control point for safe recovery; `dev` output should be treated as input material, not completion evidence.
- Catalog parity is both quantity and quality: importing all 14 workflows must preserve workflow richness, not reduce them to simplified placeholders.
- Alias workflow execution must consume `aliasWorkflowId` once and normalize target-requiring subcommands (`status`, `continue`, `pause`, `abort`, `validate`) to `<subcommand> <alias-id>` so alias routing stays deterministic and non-recursive.
- Alias routing fix now consumes `aliasWorkflowId` via a single re-entry path and forces target-requiring alias subcommands to the alias workflow ID even when an explicit target is passed, preventing recursive/override hazards.
- A dedicated `/create-workflow <id>` command can stay thin by routing directly to existing `/workflow create <id>` runtime behavior, while scaffolding `.kord/workflows/README.md` makes authoring discoverable without introducing duplicate creation logic.
- Builtin workflow source-of-truth now lives in shipped YAML assets under `src/features/builtin-workflows/`, with registry and scaffolder loading those files directly to keep runtime and scaffold defaults aligned.
- Adaptation planning is now explicit per capability row: preserve only what is already stable, adapt Synkra semantics into Kord where compatible, intentionally replace inline builtin workflow sources with YAML assets, and mark missing runtime/import semantics as not yet implemented.
- The 14-workflow Synkra catalog must be treated as a semantic baseline, not a naming checklist; parity requires metadata/phase/elicit/validation behavior retention through importer and runtime.
- Wave sequencing should prioritize importer + adaptation reports and asset-backed builtins before claiming progress on prompt assembly and full fan-out/gate execution semantics.
- Mixed guidance storage (Markdown references first, inline YAML notes second) improves authoring maintainability and supports reusable prompt assets without inflating workflow definitions.
- Workflow schema v1 now preserves Synkra-style structure declaratively (metadata/inputs/resources/artifact refs/skip rules/workflow-local allowlists) while still hard-requiring `schema_version` and existing step intents.
- Synkra import is now real and project-local: `/workflow import synkra <path-or-id>` adapts catalog YAML into `.kord/workflows/` and emits per-workflow adaptation reports under `docs/kord/workflows/`, preserving rich step/source semantics via `metadata.synkra` while explicitly reporting non-executable constructs.
- Synkra importer now materializes the full 14-workflow catalog into `src/features/builtin-workflows/` and emits per-workflow adaptation reports under `docs/kord/workflows/` so unsupported runtime constructs stay explicit instead of silently dropped.
- Restricting `/workflow import synkra` to `<workflow-id|all>` keeps import behavior deterministic against the Synkra catalog root while still writing full adapted YAML into `.kord/workflows/` and per-workflow adaptation reports into `docs/kord/workflows/`.
- Synkra import foundation is now command-real in the Kord workflow engine (`/workflow import synkra <workflow-id|all>`), with source-rich adaptation preserved into `.kord/workflows/` plus per-workflow `docs/kord/workflows/import-report-<id>.md` parity reports for unchanged fields, substitutions, unsupported constructs, and handling notes.
- Keeping a full `metadata.synkra.raw_workflow` snapshot in imported definitions preserves source fidelity for later runtime-parity work without blocking today’s `.kord/workflows/` import foundation.
- Focused importer verification should assert both side effects (`.kord/workflows/*.yaml` and `docs/kord/workflows/import-report-*.md`) and user-visible command contract (`/workflow import synkra <workflow-id|all>`) to prevent regressions back to stub semantics.
- Task 10 foundation now landed: `/workflow import synkra <workflow-id|all>` imports from Synkra catalog into `.kord/workflows/` with full adaptation reports under `docs/kord/workflows/import-report-<id>.md`, replacing the stub and making command behavior real with focused test coverage.
- Synkra workflow importer preserves complete source richness through `metadata.synkra` blocks (raw_workflow, source_step, project_types, phases, flow_diagram, decision_guidance, handoff_prompts) while generating human-readable adaptation reports that explicitly document unchanged fields, Kord-specific substitutions, unsupported constructs, and handling notes for transparency and future runtime parity work.
- Task 10 retry succeeded by fixing missing `resolve` import and confirming `.kord/workflows/` target path; importer was already implemented but blocked by a single missing Node.js path import, demonstrating that prior retries may have landed partial implementations requiring only minor fixes rather than full rewrites.
- Importing by parsing `workflow.id` from Synkra catalog YAML (instead of assuming filename == id) makes `/workflow import synkra <workflow-id|all>` resilient to renamed files while preserving the same `.kord/workflows/` + adaptation-report side effects.
- A stable Task 10 test fixture should mirror real Synkra IDs (`greenfield-fullstack`, `brownfield-discovery`) and assert both artifacts (`.kord/workflows/*.yaml`) and adaptation reports (`docs/kord/workflows/import-report-*.md`) so importer regressions are caught at command-contract level.
- Enforcing `workflow-id|all` shape in `/workflow import synkra` (rejecting path-like targets) keeps import behavior deterministic to catalog IDs while preserving the same `.kord/workflows/` + report side effects.
- Filtering `import synkra all` to YAML files that expose `workflow.id` prevents non-workflow catalog files from generating junk imports, while `metadata.synkra.raw_document` retains top-level source fidelity for adaptation auditing.
- Sorting `/workflow import synkra all` targets by `workflow.id` makes workflow-set adaptation output deterministic while preserving the same side effects (`.kord/workflows/*.yaml` + `docs/kord/workflows/import-report-*.md`) for stable tests and repeatable operator runs.
- For Task 10 foundation, deduplicating repeated `workflow.id` entries during `/workflow import synkra all` avoids overwrite ambiguity while keeping deterministic first-match import selection and explicit operator-facing notice output.
- Copying real Synkra YAML fixtures (`greenfield-fullstack`, `brownfield-discovery`) into a temp test catalog keeps `/workflow import synkra all` contract tests realistic while avoiding permanent dependence on an external absolute path at assertion time.
- For Task 10 foundation, keeping importer acceptance scoped to catalog ID selection (`<workflow-id|all>`) plus mandatory dual-artifact verification (`.kord/workflows/*.yaml` and `docs/kord/workflows/import-report-*.md`) is sufficient to declare the command real without prematurely implementing JIT prompt assembly or full runtime parity.
- Foundation lock-in: treat `/workflow import synkra <workflow-id|all>` as complete when it deterministically resolves catalog `workflow.id` entries, writes adapted YAML to `.kord/workflows/`, and emits matching `docs/kord/workflows/import-report-<id>.md` artifacts with unchanged/substitution/unsupported/handling sections.
- Focused Task 10 regression checks should verify not only artifact existence but also source-rich carryover (`metadata.synkra.raw_workflow` and decision/project metadata) so importer behavior cannot quietly degrade back to shallow/stub output.
